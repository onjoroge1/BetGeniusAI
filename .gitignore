# Secrets — NEVER commit
.env
.env.local
.env.*
!.env.example          # keep the placeholder template
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Model backups (timestamped, large binaries)
artifacts/models/*_backup_*/

# Operational/transient state (regenerated by archive + training runs)
scripts/archive_manifest.json
scripts/cold_archive_manifest.json
scripts/temporal_holdout_result.json
scripts/wc2026_track1_manifest.json

# OS junk
.DS_Store

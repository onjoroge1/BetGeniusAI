love it. here’s the clean way to wrap Phase 0/1 **and** unblock Phase 2 fast.

# What I’ll do now

I’ll proceed with your plan to **document Phase 0/1** and mark Phase 2 as “in progress, pending data expansion.” Below is paste-ready content plus the exact backlog to expand data and resume 2.4–2.6.

---

# Paste into `replit.md` (Phase 0/1 wrap-up)

## ✅ Phase 0 — Metrics Hygiene (Complete)

* Defensive **probability normalization** on read (fixed V1 bug where sums were 1.08–1.22).
* **Pre-kickoff filter** enforced in evaluation.
* Added **ECE** + **reliability curves** (global + per-league).

**Current (paired, n≈240):**

* V1 (market consensus): Brier 0.6081, LogLoss 1.0190, Hit 48.3%, ECE 0.0809
* V2 (market-delta ridge): **Brier 0.6057**, **LogLoss 1.0157**, Hit 48.3%, ECE 0.0811

## ✅ Phase 1 — Calibration & Constraints (Complete)

* **Temperature scaling** (LODO CV): T≈1.0 → no benefit → **skip**.
* **Blend/KL/Δτ sweep (32 cfgs)**: no config beat raw V2 → **use raw V2**.

**Conclusion:** V2 is **already optimal** under current features & training; next gains must come from **better features + stronger learner** (Phase 2).

## 🏗️ Phase 2 — Status (Infrastructure built, data limited)

* Built tables/jobs: `market_features`, `elo_ratings`, `team_features`.
* Fixed ID sync: **training_matches → fixtures → match_results** 3-table bridge.
* **Training matrix** assembled (137 samples, single-league heavy) → too small for LGBM without overfitting.

**Blocker:** Need more labeled matches with complete pre-kick features (multi-league).

---

# “Next Up” Backlog (to unblock 2.4–2.6)

## A) Data Expansion (priority)

**Goal:** get to **≥1,000** labeled matches (multi-league) with: opening & last pre-kick odds, drift/dispersion/volatility, team IDs, outcomes.

1. **Backfill fixtures + results** (90’ FT only)

```sql
-- sanity: how many results per league/date?
SELECT league, DATE(kickoff_at) d, COUNT(*) 
FROM fixtures f JOIN match_results mr USING(match_id)
GROUP BY 1,2 HAVING COUNT(*)>5 ORDER BY d DESC;
```

* Expand date window: **last 12–18 months** across EPL, LaLiga, Serie A, Bundesliga, Ligue 1, MLS, Liga MX, Eredivisie, Portugal, Brazil Série A, Argentina, etc.

2. **Backfill odds snapshots**

* Ingest **opening** + all **pre-kick** snapshots per match (from TheOddsAPI / API-Football).
* Ensure each snapshot row is **pre-kick** and **normalized** (H/D/A sum=1).

3. **Recompute `market_features`**

* Remove any hidden over-filters (e.g., don’t require 2+ unique timestamps; allow 1+).
* Keep: `p_open_*`, `p_last_*`, `prob_drift_*`, `book_dispersion`, `odds_volatility`.

4. **Recompute `elo_ratings`**

* Use **real team_ids** (no hashing).
* Join through **fixtures** for dates; forbid post-kick joins.

**Acceptance to resume 2.4:**

* ≥1,000 matches; ≥10 leagues; **feature coverage ≥95%**; draw share per league in **22–30%** band.

## B) Dataset Builder – safer joins

* Build **two** matrices:

  * **Market-only** (robust fallback): uses market_features + outcomes (broadest coverage).
  * **Full** (enriched): adds ELO/form/rest/manager flags (when available).
* Deterministic imputation (league medians). Hard-fail pipeline if coverage <95%.

**Key SQL to find leaks/over-filters**

```sql
-- Overlap audit
WITH m AS (
  SELECT DISTINCT match_id FROM market_features
), r AS (
  SELECT DISTINCT match_id FROM match_results
), f AS (
  SELECT DISTINCT match_id FROM fixtures WHERE kickoff_at IS NOT NULL
)
SELECT 
  (SELECT COUNT(*) FROM m) AS mkt_rows,
  (SELECT COUNT(*) FROM r) AS res_rows,
  (SELECT COUNT(*) FROM f) AS fix_rows,
  (SELECT COUNT(*) FROM m JOIN r USING(match_id)) AS mkt∩res,
  (SELECT COUNT(*) FROM m JOIN r USING(match_id) JOIN f USING(match_id)) AS full_overlap;
```

## C) LightGBM (2.4) & Shadow A/B (2.5)

* Train **market-only LGBM** first (to validate sample size helps), then the **full** model when team features catch up.
* Temporal CV only; per-league temperature after training.
* Shadow both vs V2 ridge; require **paired log-loss ↓** and **ECE ≤ 0.03** global (≤0.05 league).

## D) Draw-Aware Guard (2.6)

* Enable only after LGBM is green; gate by high-entropy + small H/A gap; verify draw-bin reliability improves without worsening log-loss.

---

# Quick tasks you can hand the agent (copy-paste)

**Task 1 — Odds backfill (last 12–18 months, 10+ leagues)**

```
Collect opening + pre-kick snapshots for each match_id in fixtures.
Write to market_snapshots; normalize H/D/A on write; ensure snapshot_ts < kickoff_at.
Rebuild market_features for all affected matches.
```

**Task 2 — Results backfill (FT 90’)**

```
Ensure match_results has FT outcomes only; map ET/Pens → 90’ result.
Audit league draw shares (expect 22–30%). Flag outliers.
```

**Task 3 — ELO rebuild**

```
Run jobs/elo_recompute.py over the expanded window using fixtures↔match_results.
Validate mean≈1500, stdev 50–100 per league; no forward-looking joins.
```

**Task 4 — Two training matrices**

```
Build market-only and full matrices; enforce feature_coverage ≥95%; no NaNs.
Export to artifacts/datasets/*.parquet.
```

**Task 5 — Train + Shadow**

```
Train LGBM on market-only → evaluate vs V2 ridge (paired).
If green, train full LGBM; deploy as v2_candidate with shadow; run /metrics compare with McNemar + bootstrap CIs.
```

---

# TL;DR call

* I’ll **update the docs** per above.
* Yes, we **should** also expand and clean the **training data**; that’s the gating item for 2.4–2.6.
* As soon as the agent backfills to ~1k+ multi-league samples, we resume LightGBM + shadow and push for the expected **−0.02 to −0.06 log-loss** and **+2–5 pts hit** lift.

If you want, I can also draft the **exact ingestion spec** (columns + normalization rules) for the odds/results backfill so your agent doesn’t guess any field names.
